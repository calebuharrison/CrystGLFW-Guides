# `CrystGLFW::ReleaseBehavior`

A `CrystGLFW::ReleaseBehavior` (or simply `ReleaseBehavior` for the purposes of this guide) is an Enum that represents the three Release Behavior options: Any, Flush, and None.